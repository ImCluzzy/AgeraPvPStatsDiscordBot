# AgeraPvP Stats Discord Bot

Discord бот для получения и отображения статистики игроков сервера AgeraPvP.

## Возможности

- 📊 Получение статистики игрока по команде `/stats <ник> <режим>`
- 🎨 Красивое оформление статистики в виде изображения
- ✅ Проверка соединения с API командой `/test`

## Установка

1. **Клонируйте репозиторий или скачайте файлы**

2. **Создайте виртуальное окружение:**

   **Windows:**
   ```bash
   # Создание виртуального окружения
   python -m venv venv
   
   # Активация виртуального окружения
   venv\Scripts\activate
   ```
   
   **Linux:**
   ```bash
   # Создание виртуального окружения
   python3 -m venv venv
   
   # Активация виртуального окружения
   source venv/bin/activate
   ```
   
   > **Примечание:** После активации виртуального окружения в начале строки терминала появится префикс `(venv)`. Для деактивации используйте команду `deactivate`.

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Создайте Discord бота:**
   - Перейдите на [Discord Developer Portal](https://discord.com/developers/applications)
   - Создайте новое приложение
   - Перейдите в раздел "Bot" и создайте бота
   - Скопируйте токен бота
   - Включите следующие привилегии:
     - MESSAGE CONTENT INTENT (в разделе Privileged Gateway Intents)
     - SERVER MEMBERS INTENT (опционально)

5. **Получите API ключ:**
   - Получите API ключ на сервере через команду /api
   - API ключ необходим для аутентификации при запросах к API

6. **Настройте переменные окружения:**
   - Скопируйте файл `env.example` в `.env` (или создайте файл `.env` вручную)
   - Откройте `.env` и заполните следующие переменные:
     ```
     BOT_TOKEN=ваш_токен_дискорд_бота
     API_KEY=ваш_api_ключ_от_сервера
     ```
   - **Важно:** Не публикуйте файл `.env` в публичных репозиториях!

7. **Пригласите бота на сервер:**
   - В Discord Developer Portal перейдите в раздел "OAuth2" → "URL Generator"
   - Выберите scope: `bot` и `applications.commands`
   - Выберите необходимые права (Bot Permissions):
     - Send Messages
     - Attach Files
     - Use Slash Commands
   - Скопируйте сгенерированную ссылку и откройте её в браузере
   - Выберите сервер и авторизуйте бота

## Запуск

**Важно:** Убедитесь, что виртуальное окружение активировано!

**Windows:**
```bash
venv\Scripts\activate
python bot.py
```

**Linux:**
```bash
source venv/bin/activate
python bot.py
```

## Использование

После запуска бота используйте следующие команды:

- `/stats <ник> <режим>` - Получить статистику игрока
  - Пример: `/stats ImCluzzy BW`
  - Доступные режимы: `BW` (BedWars), `Duels`
  
- `/profile <ник>` - Получить профиль игрока
  - Пример: `/profile ImCluzzy`
  - Показывает информацию о профиле и изображение скина
  
- `/punishments` - Получить статистику наказаний
  - Показывает общую статистику банов и мутов
  
- `/staff` - Получить список онлайн стаффа
  - Показывает список всех онлайн администраторов и модераторов
  
- `/online` - Получить общее количество онлайн игроков
  - Показывает текущее количество игроков на сервере
  
- `/test` - Проверить соединение с API

## Структура проекта

```
.
├── bot.py                 # Основной файл бота
├── core/                  # Основные модули
│   ├── __init__.py
│   └── api_client.py      # Клиент для работы с API
├── generators/            # Генераторы изображений
│   ├── __init__.py
│   ├── image_generator.py        # Генератор изображений со статистикой
│   ├── profile_generator.py      # Генератор изображений профиля
│   └── punishments_generator.py # Генератор изображений статистики наказаний
├── requirements.txt       # Зависимости проекта
├── .env                   # Переменные окружения (создайте сами, не публикуйте!)
├── env.example            # Пример файла с переменными окружения
└── README.md             # Документация
```

## API

Бот использует API AgeraPvP: `http://api.agerapvp.club/`

Основной эндпоинт: `/v1/player/stats/{name}/{mode}`

## Требования

- Python 3.8+
- Discord.py 2.3.0+

## Примечания

- Убедитесь, что у бота есть права на отправку сообщений и файлов в каналах
- Бот использует слэш-команды (slash commands), которые требуют синхронизации с Discord
- При первом запуске команды могут быть недоступны несколько минут (время синхронизации)

## Дизайн

Дизайн разработан: [who Koncept](https://t.me/whokonceept)

## Поддержка

При возникновении проблем проверьте:
1. Правильность токена бота (BOT_TOKEN)
2. Правильность API ключа (API_KEY) - должен быть получен от администратора сервера
3. Наличие необходимых прав у бота на сервере
4. Доступность API AgeraPvP
5. Правильность написания ника и режима игры
6. Активацию виртуального окружения перед запуском

